package com.portfolio.finance.repository;

import com.portfolio.finance.domain.ProjectCashFlow;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectCashFlowRepository extends JpaRepository<ProjectCashFlow, Long> {
}
